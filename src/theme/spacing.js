/**
 * Escala de espaçamento (múltiplos de 4, padrão usado pela maioria dos
 * design systems mobile/iOS). Usar sempre `spacing.X` em vez de números soltos.
 */
export const spacing = {
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 20,
  xxl: 24,
  xxxl: 32,
};

export const radius = {
  sm: 8,
  md: 12,
  lg: 16,
  xl: 20,
  full: 999,
};

/** Área mínima de toque (WCAG 2.2 / Apple HIG) para botões e ícones clicáveis. */
export const touchMin = 44;
